node_modules
.env

# Anchored with a leading slash. Unanchored "data/" matches a directory of that
# name at ANY depth, which silently excluded server/src/data/ — application
# source that every route imports — from the repository.
/data/
/uploads/*
!/uploads/.gitkeep
